---
const { icon, title, description, link } = Astro.props;
---

<div class="bg-white rounded-2xl shadow-md border border-gray-200 p-6 hover:shadow-lg transition duration-300 flex flex-col dark:bg-slate-900 dark:border-slate-700">
  <div class="flex flex-col items-center">
        <div class="w-14 h-14 flex items-center justify-center bg-orange-100 text-white rounded-full shadow-md dark:bg-slate-200">
          <img src={icon} alt={title + " icon"} class="w-6 h-6" />
        </div>
        <h3 class="text-lg font-semibold mt-4 text-gray-800 dark:text-slate-50">{title}</h3>
        <p class="text-gray-600 text-sm dark:text-slate-300">{description}</p>
      </div>
</div>